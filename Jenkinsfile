pipeline {
    agent none
    stages {
        stage('build') {
            steps {
                echo "build step"
                }
        }
        stage('deploy') {
            steps {
                sh 'echo deploy'
            }
        }
    }
}
